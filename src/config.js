const config = {
  // I guess consumer keys identify your app
  consumer_key: '',
  consumer_secret: '',
  // while access tokens authorize the app to use your twitter account
  // better worded: allows app to make API requests on your own accounts behalf.
  access_token: '',
  access_token_secret: '',
  timeout_ms: 5000
};

module.exports = config;
